import React from 'react';
import ReactDOM from 'react-dom';

window.addEventListener('load', function () {
  ReactDOM.render(
    <img src="images/railtracks.png"/>,
    document.getElementById('app')
  );
});
